# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration - Choose one option below:

# Option 1: Local MongoDB (requires MongoDB installation)
# MONGODB_URI=mongodb://localhost:27017/SampleBlogs

# Option 2: MongoDB Atlas (recommended for quick testing)
# Get your connection string from MongoDB Atlas and replace the placeholder below
MONGODB_URI=mongodb://localhost:27017/SampleBlogs
retryWrites=true&w=majority

# Option 3: Other MongoDB services (Railway, etc.)
# MONGODB_URI=your_mongodb_connection_string_here

# Application Configuration
API_PREFIX=/api
