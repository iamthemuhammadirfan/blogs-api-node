# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration
# For local MongoDB (install MongoDB first):
MONGODB_URI=mongodb://localhost:27017/SampleBlogs

# For MongoDB Atlas (recommended for testing):
# Replace with your MongoDB Atlas connection string
# MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/SampleBlogs?retryWrites=true&w=majority

# Or use a free MongoDB service like MongoDB Atlas or Railway

# Application Configuration
API_PREFIX=/api
